name: splash_screen
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.0.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^0.8.3+1
  translator: ^0.1.7
  tflite: ^1.1.2




dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/tomato.png
    - assets/bg.png
    - assets/model.tflite
    - assets/labels.txt


flutter_localizations:
    sdk: flutter
